id = "market-expression"
name = "Market Expression"
bundle_id = "com.sympoies.market-expression"
version = "1.2.4"
script_filter = "script_filter.sh"
action = "action_copy.sh"
rust_binary = "market-cli"
assets = ["src/assets/icon.png"]

[env]
# Optional executable path override for local/debug runtime.
MARKET_CLI_BIN = ""
# Default fiat used by market-cli expr when target fiat is omitted in query.
MARKET_DEFAULT_FIAT = "USD"
# Optional FX cache TTL. Empty keeps the built-in 1d default.
MARKET_FX_CACHE_TTL = ""
# Optional crypto cache TTL. Empty keeps the built-in 5m default.
MARKET_CRYPTO_CACHE_TTL = ""
# Toggle empty-query favorites rows. Set 0/false/off to show only the prompt row.
MARKET_FAVORITES_ENABLED = "1"
# Ordered favorites list shown when query is empty. Supports comma/newline separated symbols.
MARKET_FAVORITE_LIST = "BTC,ETH,EUR,JPY"

[alfred]
min_alfred = "5"
min_macos = "13.0"
